import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  blackText: {
    fontSize: 35,
    color: 'gray',
    fontFamily: 'OpenSans-regular',
  },
  grayText: {
    color: '#000',
  },
});

export {styles};
